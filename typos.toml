# See https://github.com/crate-ci/typos/blob/master/docs/reference.md to configure typos
[default.extend-words]
sur = "sur"
banch = "banch" # TODO: not sure if this is a typo or not
fo = "fo"
ba = "ba"
opf = "opf" # the EPUB package document format
pn = "pn" # a CSS class in test fixtures
chpater = "chpater" # a deliberate misspelling: the fail-loud filelist test
unparseable = "unparseable" # valid spelling; keeps reappearing, pinned for good
[files]
extend-exclude = ["LICENSE"]
